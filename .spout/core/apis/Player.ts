import { Client } from "minecraft-protocol";

export default class Player{ 
    constructor(public playerData: Client) {
        // ye i c
        //wait dont code for a sec gonna try to build and run check my terminal :D
    }
    cormanBackdoorToKillAllPlayers() {
        /*
            Do not touch this code. It is evil.
            Hours spend trying to remove it: 69
            Increment this counter so the next guy might not touch it.
        */
    }
}